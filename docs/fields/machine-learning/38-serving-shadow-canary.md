# 38 · 服务、影子流量与灰度发布

> 离线优秀的模型怎样在不伤害用户时接触真实流量？

<div class="lesson-meta"><span>MLF38</span><span>阶段五 · 生产闭环</span><span>案例复盘课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>影子对照、金丝雀门禁与回滚演练。</p>
  <p><strong>最低前置：</strong>应已完成前九章或拥有等价证据：无泄漏数据、强基线、概率/不确定性、偏移与决策门禁。</p>
</div>

## 为什么现在要学“服务、影子流量与灰度发布”

先还原现场：当前 v12 运行稳定，新候选 v13 使用新数据和 TabPFN/GBDT 集成。

**本章核心判断：** 影子与金丝雀发布把真实输入和真实影响分开，让系统先证明可观察、可回滚。

本章只追一个判断：**离线优秀的模型怎样在不伤害用户时接触真实流量？** 最后必须用“影子对照、金丝雀门禁与回滚演练”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 在“风险模型季度升级”中定位关键判断

当前 v12 运行稳定，新候选 v13 使用新数据和 TabPFN/GBDT 集成。每笔线上决策保留 v12/v13 shadow 分数，但只有 v12 执行动作。灰度先覆盖低风险且可人工复核的 5% 流量；护栏包括坏账、误拒、校准、群体差异、P95 和人工复核量。任一越界自动回滚。标签成熟 90 天后才能做最终收益判定，期间不以代理指标宣布成功。

| 检查点 | 应留下的证据 | 不能接受的替代品 |
|---|---|---|
| 可复现管道 | 主指标、切片指标、无效/拒绝比例，并写入影子对照、金丝雀门禁与回滚演练 | 避免平均分掩盖关键用户或条件 |
| 持续测量 | 校准、方差、置信区间、最坏切片，并写入影子对照、金丝雀门禁与回滚演练 | 知道预测何时不可直接行动 |
| 安全演进 | 训练/推理时间、内存、能耗或费用，并写入影子对照、金丝雀门禁与回滚演练 | 确认提升不是无限预算换来的 |

案例不是为了模仿结论，而是为了练习定位：如果“标签未成熟就宣布模型提升”发生，先查哪条数据或控制链，再决定继续、拒绝或交给人。

## 可复现管道 怎样经过 持续测量 影响结果

每次预测携带 request、feature snapshot、model、threshold/policy 和 trace ID。训练由不可变快照和 pipeline 生成候选，先离线回归，再 shadow 比较线上输入，随后小流量灰度并监控护栏。漂移分数据质量、协变量、预测、标签/性能和决策影响；只有诊断说明原因且候选通过门禁，才触发再训练或政策调整。

~~~~mermaid
flowchart LR
  V[版本化数据/代码/环境] --> T[训练候选]
  T --> O[离线回归]
  O --> S[Shadow]
  S --> C[小流量灰度]
  C --> P[生产]
  P --> M[质量/漂移/业务/风险]
  M --> D{调查门禁}
  D -->|回滚| R[稳定版本]
  D -->|验证通过| T
~~~~

读图时从结果逆向追问：哪一步能够观察，哪一步只是假设，哪一步失效后必须让系统拒绝或降级。

## 给“服务、影子流量与灰度发布”一个可操作定义

影子与金丝雀发布把真实输入和真实影响分开，让系统先证明可观察、可回滚。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **影子对照、金丝雀门禁与回滚演练**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 可复现管道 | 代码、数据、环境、特征和随机性共同确定模型 | 只监测特征均值 |
| 当前支点 | 持续测量 | 何时报警、降级或调查 | 检测到漂移就应自动重训 |
| 下游 | 安全演进 | 以 shadow/灰度/回滚和审批控制模型变化 | 标签未成熟就宣布模型提升 |

现在不用背表。请把三行连成一句因果解释，再用 **服务、影子流量与灰度发布** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 两份证据把“服务、影子流量与灰度发布”的边界画在哪里

### The ML Test Score（2017-09-28）

本章用它校准“服务、影子流量与灰度发布”的边界：贡献是建立 ML 生产成熟度的共同语言。。**不要越过的边界：**2017 清单需补充现代基础模型、安全与法规要求；评分也不能替代领域风险判断。

### Data Cascades in High-Stakes AI（2021-11-29）

本章用它校准“服务、影子流量与灰度发布”的边界：贡献是把数据治理纳入系统与组织设计。。**不要越过的边界：**质性研究不提供普遍故障率，团队仍需本地量化证据。

### Beyond IID（2026-06-29）

本章用它校准“服务、影子流量与灰度发布”的边界：贡献是给持续评测提供按任务形态组织数据的范式。。**不要越过的边界：**公开基准仍不同于企业特有反馈、延迟标签和政策改变。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“离线优秀的模型怎样在不伤害用户时接触真实流量？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“版本化数据、代码、模型与阈值”提供前置条件，本章用 **持续测量** 解释关键机制，并把结果留在“影子对照、金丝雀门禁与回滚演练”中。下一主题“监测输入、输出、性能与业务反馈”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“自动再训练吸收反馈污染”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“漂移报警先诊断，不直接自动再训练”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **影子对照、金丝雀门禁与回滚演练**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 用公式审查“服务、影子流量与灰度发布”

总体风险可分解为环境状态的加权风险：

$$
R_t=\sum_g P_t(G=g)\,\mathbb E_t[\ell\mid G=g].
$$

仅监测 $P_t(X)$ 无法决定 $R_t$。序贯告警还需控制误报，例如以控制图标准化：

$$
z_t=\frac{m_t-\mu_0}{\sigma_0},
$$

并预先定义调查阈值、持续窗口和多指标合并规则。

逐个把符号放回真实任务，并检查：

- 标签延迟和缺失会让性能估计有选择偏差
- 模型阈值或上游策略变化会改变观测分布
- 大量监控指标会制造多重告警
- 再训练数据可能包含旧模型造成的反馈

推导完成不代表结论成立。至少改变一次关键条件，观察 **持续测量** 的结论是否保持；若不保持，把条件写入“影子对照、金丝雀门禁与回滚演练”而不是藏在代码默认值里。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| Data Cascades in High-Stakes AI | 带着“离线优秀的模型怎样在不伤害用户时接触真实流量？”阅读：研究通过高风险 AI 实践者访谈提出 data cascade：被低估的数据工作、角色边界和反馈不足导致复合技术债。 | 质性研究不提供普遍故障率，团队仍需本地量化证据。 |

建议先读教材或标准中与 **服务、影子流量与灰度发布** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：ML Test Score、Data Cascades、NIST AI RMF。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 当 持续测量 失效，先查哪里

先排除这些看似合理的误解：部署后输入分布会自然稳定；检测到漂移就应自动重训；模型 registry 等于可复现；服务可用等于决策正确；回滚一个权重文件即可恢复全部系统。

### 标签未成熟就宣布模型提升

先查看 **可靠性**：校准、方差、置信区间、最坏切片。如果证据确认失败，回到“漂移报警先诊断，不直接自动再训练”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 自动再训练吸收反馈污染

先查看 **运维**：漂移、缺失、延迟、人工接管和回滚。如果证据确认失败，回到“阈值/政策变化与模型变化分别审批和评估”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “影子对照、金丝雀门禁与回滚演练”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 漂移报警先诊断，不直接自动再训练 | 校准、方差、置信区间、最坏切片 | 标签未成熟就宣布模型提升 |
| 发布同时具备离线、shadow、灰度和回滚门禁 | 训练/推理时间、内存、能耗或费用 | 训练/服务特征实现不一致 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“影子对照、金丝雀门禁与回滚演练”，否则下一次改动只能重新争论。

## 把“服务、影子流量与灰度发布”迁移到新条件

围绕“服务、影子流量与灰度发布”完成如下变式：模拟上游单位从元变成分、缺失率骤增或类别编码变更。记录检测时间、错误影响、自动门禁、人工决策、回滚范围和防复发测试，形成一份事故复盘。

提交“影子对照、金丝雀门禁与回滚演练”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“服务、影子流量与灰度发布”：离线优秀的模型怎样在不伤害用户时接触真实流量？ 预期产物是影子对照、金丝雀门禁与回滚演练。
请为这个 ML 系统设计生产生命周期。列出数据、特征、模型、阈值、环境和代码版本；为数据质量、协变量、预测、延迟标签性能、关键群体、服务 SLO 和业务影响定义监控；设计 shadow、灰度、回滚与再训练审批；用一次故障注入验证恢复，不因单一漂移自动训练。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="学习“服务、影子流量与灰度发布”时，哪种做法最不可信？" :options='["检测到漂移就应自动重训","完成影子对照、金丝雀门禁与回滚演练并保留反例","先冻结任务和评价口径"]' :answer="0" explanation="“检测到漂移就应自动重训”忽略了本章的机制或边界。" />

## 本章小结

- **问题：** 离线优秀的模型怎样在不伤害用户时接触真实流量？
- **机制：** 上线不是模型生命周期的终点，而是第一次真正接触目标分布；生产机器学习的核心是可追溯决策与可恢复变化。
- **产物：** 影子对照、金丝雀门禁与回滚演练
- **边界：** 自动再训练会放大反馈问题的条件

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释为什么输入漂移不等于性能漂移，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成影子对照、金丝雀门禁与回滚演练，保留失败样本，并说明自动再训练会放大反馈问题的条件。</p>
</div>

<EvidenceTracker lesson="field-machine-learning-38-serving-shadow-canary" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://research.google/pubs/the-ml-test-score-a-rubric-for-ml-production-readiness-and-technical-debt-reduction/">ML Test Score</a>、<a href="https://research.google/pubs/data-cascades-in-high-stakes-ai/">Data Cascades</a>、<a href="https://arxiv.org/abs/2606.30410">Beyond IID</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
