# 24 · 嵌套交叉验证与无泄漏调参

> 验证集被反复使用后为什么也会过拟合？

<div class="lesson-meta"><span>MLF24</span><span>阶段三 · 表示、训练与选型</span><span>跟做实验课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>嵌套 CV、搜索预算与选择协议。</p>
  <p><strong>最低前置：</strong>需要能读损失函数、训练/验证曲线，并理解模型选择必须隔离最终测试集。</p>
</div>

## 为什么现在要学“嵌套交叉验证与无泄漏调参”

这是一章可以重放的实验。先冻结输入、基线和停止条件，再让结果回答问题，而不是边跑边改结论。

**本章核心判断：** 超参数搜索也是训练过程；嵌套验证把模型选择和最终性能估计隔离，避免验证集过拟合。

本章只追一个判断：**验证集被反复使用后为什么也会过拟合？** 最后必须用“嵌套 CV、搜索预算与选择协议”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 把“嵌套交叉验证与无泄漏调参”做成可重放实验

实验标题：**嵌套 CV、搜索预算与选择协议**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“验证集被反复使用后为什么也会过拟合？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 只报告最好随机种子。

### 2. 执行本章关键动作

增加无关超参数扩大搜索空间，观察最佳验证分数如何虚高

- **必须保存：** 漂移、缺失、延迟、人工接管和回滚。
- **停止信号：** 用不合法增强编码错误不变性。

### 3. 主动注入反例

构造“只报告最好随机种子”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 把优化没收敛误判成模型容量不足。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 嵌套 CV、搜索预算与选择协议及其适用范围。
- **停止信号：** 无限增加 trial 追逐验证噪声。

## 给“嵌套交叉验证与无泄漏调参”一个可操作定义

超参数搜索也是训练过程；嵌套验证把模型选择和最终性能估计隔离，避免验证集过拟合。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **嵌套 CV、搜索预算与选择协议**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 模型选择 | 在有限搜索预算下无偏比较配置 | 把优化没收敛误判成模型容量不足 |
| 当前支点 | 优化 | 未收敛还是泛化失败 | 交叉验证后不再需要独立测试 |
| 下游 | 正则化 | 通过惩罚、约束、噪声或早停限制有效容量 | 无限增加 trial 追逐验证噪声 |

现在不用背表。请把三行连成一句因果解释，再用 **嵌套交叉验证与无泄漏调参** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“验证集被反复使用后为什么也会过拟合？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“正则化如何购买偏差并减少方差”提供前置条件，本章用 **优化** 解释关键机制，并把结果留在“嵌套 CV、搜索预算与选择协议”中。下一主题“把特征工程写成可重放流水线”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“把优化没收敛误判成模型容量不足”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“若调参净收益小于方差/成本，采用稳定默认基线”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **嵌套 CV、搜索预算与选择协议**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 把“嵌套交叉验证与无泄漏调参”放回 40 章依赖链

~~~~mermaid
flowchart LR
  A[上一主题：正则化如何购买偏差并减少方差] -->|提供前置| B[当前：嵌套交叉验证与无泄漏调参]
  B -->|留下：嵌套 CV、搜索预算与选择协议| C[下一主题：把特征工程写成可重放流水线]
  C --> D{能否支持：嵌套 CV、搜索预算与选择协议}
  D -->|不能| E[回到假设与数据]
  D -->|能够| F[进入下一主题]
~~~~

这张图不是通用真理，而是本章的待验证模型。优化解决训练目标，正则化塑造可选解，模型选择估计哪个解能泛化；三者不能用一条下降的 loss 曲线代替。 如果输入、环境或责任边界改变，箭头也要重新检查。

## “验证集被反复使用后为什么也会过拟合？”依赖哪些前提

本章暂时不展开整套推导，但必须把判断拆成可以检查的前提：

| 顺序 | 必须成立的前提 | 对应控制 |
|---|---|---|
| 1 | 偏差—方差分解依赖损失和数据假设；在“嵌套交叉验证与无泄漏调参”中必须显式核对 | 搜索空间、预算、停止规则在运行前冻结 |
| 2 | 正则强度与特征尺度相互作用；在“嵌套交叉验证与无泄漏调参”中必须显式核对 | 实验平台记录所有试验而非只保留冠军 |
| 3 | 搜索空间大小是实验自由度；在“嵌套交叉验证与无泄漏调参”中必须显式核对 | 外层评测运行完整预处理和调参算法 |
| 4 | 外层测试不能参与早停、特征选择或搜索空间修改；在“嵌套交叉验证与无泄漏调参”中必须显式核对 | 若调参净收益小于方差/成本，采用稳定默认基线 |

这四项共同约束“验证集被反复使用后为什么也会过拟合？”。任何一项只能靠猜测回答时，都应降低结论强度或补实验。

## 两份证据把“嵌套交叉验证与无泄漏调参”的边界画在哪里

### Tuned Models Often Do Not Improve upon Default Configurations（2019-11-13）

本章用它校准“嵌套交叉验证与无泄漏调参”的边界：贡献是把“调参一定有价值”变成可检验命题。。**不要越过的边界：**默认值与软件版本不断变化，具体排名不能外推到所有任务和现代系统。

### Adapting Prediction Sets to Distribution Shifts Without Labels（2025-07-13）

本章用它校准“嵌套交叉验证与无泄漏调参”的边界：它展示测试时适配可以改变可靠性，但同时增加选择与假设层。。**不要越过的边界：**保证不再是原始交换性下的无条件结论；模型不确定性失真时调整也会失败。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| Adapting Prediction Sets to Distribution Shifts Without Labels | 带着“验证集被反复使用后为什么也会过拟合？”阅读：ECP/EA-CP 根据基础模型自身不确定性调整 conformal score，并在多个大规模数据集和神经架构上比较无监督适配基线。 | 保证不再是原始交换性下的无条件结论；模型不确定性失真时调整也会失败。 |

建议先读教材或标准中与 **嵌套交叉验证与无泄漏调参** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：PRML、Bayesian Optimization、Nested CV。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## “嵌套 CV、搜索预算与选择协议”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 若调参净收益小于方差/成本，采用稳定默认基线 | 漂移、缺失、延迟、人工接管和回滚 | 只报告最好随机种子 |
| 搜索空间、预算、停止规则在运行前冻结 | 主指标、切片指标、无效/拒绝比例 | 把优化没收敛误判成模型容量不足 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“嵌套 CV、搜索预算与选择协议”，否则下一次改动只能重新争论。

## 当 优化 失效，先查哪里

先排除这些看似合理的误解：训练 loss 低表示模型好；正则化只等于 L2；贝叶斯优化自动找到真实最优；交叉验证后不再需要独立测试；更多 trial 永远提高结论质量。

### 只报告最好随机种子

先查看 **运维**：漂移、缺失、延迟、人工接管和回滚。如果证据确认失败，回到“若调参净收益小于方差/成本，采用稳定默认基线”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 把优化没收敛误判成模型容量不足

先查看 **任务**：主指标、切片指标、无效/拒绝比例。如果证据确认失败，回到“搜索空间、预算、停止规则在运行前冻结”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## 把“嵌套交叉验证与无泄漏调参”迁移到新条件

围绕“嵌套交叉验证与无泄漏调参”完成如下变式：在同一数据上逐步把搜索次数从 10 增到 200，比较最佳验证分数与未触碰外部测试分数；画出二者差距并解释为什么更多搜索可能只优化噪声。

提交“嵌套 CV、搜索预算与选择协议”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“嵌套交叉验证与无泄漏调参”：验证集被反复使用后为什么也会过拟合？ 预期产物是嵌套 CV、搜索预算与选择协议。
请设计这项模型选择实验。区分训练优化、容量正则和超参数选择；冻结搜索空间、预算和停止条件；用嵌套或时间外评测运行完整选择算法；报告所有 trial、随机种子、置信区间和计算成本，并与默认配置及简单基线比较。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="什么最能证明你完成了“嵌套交叉验证与无泄漏调参”？" :options='["收藏三篇论文","交付嵌套 CV、搜索预算与选择协议并说明适用条件与失败样本","得到一次成功截图"]' :answer="1" explanation="可检查产物、边界和失败样本共同构成掌握证据。" />

## 本章小结

- **问题：** 验证集被反复使用后为什么也会过拟合？
- **机制：** 优化解决训练目标，正则化塑造可选解，模型选择估计哪个解能泛化；三者不能用一条下降的 loss 曲线代替。
- **产物：** 嵌套 CV、搜索预算与选择协议
- **边界：** 调参分数不能直接作为最终性能的原因

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释优化误差、泛化误差和选择偏差，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成嵌套 CV、搜索预算与选择协议，保留失败样本，并说明调参分数不能直接作为最终性能的原因。</p>
</div>

<EvidenceTracker lesson="field-machine-learning-24-nested-cv-hpo" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/1910.11757">Hyperparameter Defaults Study</a>、<a href="https://proceedings.mlr.press/v286/kasa25a.html">ECP</a>、<a href="https://www.automl.org/book/">AutoML Book</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
