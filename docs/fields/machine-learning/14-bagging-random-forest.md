# 14 · Bagging 与随机森林为什么降方差

> 许多相关的树平均以后，误差会在什么条件下下降？

<div class="lesson-meta"><span>MLF14</span><span>阶段二 · 可解释强基线</span><span>原理推导课</span><span>证据更新至 2026-08-30</span></div>

<div class="draft-status">
  <strong>内容状态：待原稿驱动重写。</strong> 当前页保留旧资料簇生成稿，只用于展示 40 主题路线和暴露待修问题；它尚未通过“原稿已获取—具体章节已读—观点已登记—教学文章人工组织”的正式章门禁，请不要把它当成完成的学习文章。
</div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>树间相关、袋外误差与方差对照。</p>
  <p><strong>最低前置：</strong>需要掌握无泄漏切分、分类/回归损失与简单概率基线。</p>
</div>

## 为什么现在要学“Bagging 与随机森林为什么降方差”

公式不是装饰。它的价值是迫使我们写清对象、假设和优化目标，再检查现实是否满足。

**本章核心判断：** Bagging 通过平均不完全相关的高方差模型降方差；树越像，平均带来的收益越小。

本章只追一个判断：**许多相关的树平均以后，误差会在什么条件下下降？** 最后必须用“树间相关、袋外误差与方差对照”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 用公式审查“Bagging 与随机森林为什么降方差”

回归树分裂可写为

$$
\min_{j,s}\left[\sum_{x_i^j\le s}(y_i-c_1)^2+\sum_{x_i^j>s}(y_i-c_2)^2\right].
$$

梯度提升更新：

$$
F_m(x)=F_{m-1}(x)+\eta h_m(x),\quad
h_m\approx-\frac{\partial \ell(y,F)}{\partial F}\bigg|_{F_{m-1}}.
$$

逐个把符号放回真实任务，并检查：

- 贪心分裂不是全局最优树
- bagging 只有在基学习器误差不完全相关时有效
- boosting 的迭代轮数与学习率不能分别孤立解释
- 类别统计必须严格保持时间/折内因果顺序

推导完成不代表结论成立。至少改变一次关键条件，观察 **Bagging** 的结论是否保持；若不保持，把条件写入“树间相关、袋外误差与方差对照”而不是藏在代码默认值里。

## 给“Bagging 与随机森林为什么降方差”一个可操作定义

Bagging 通过平均不完全相关的高方差模型降方差；树越像，平均带来的收益越小。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **树间相关、袋外误差与方差对照**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 决策树 | 以分裂规则递归划分特征空间并在叶子估计 | 把高基数 ID 当正常类别 |
| 当前支点 | Bagging | 稳定性收益是否值得并行成本 | 随机森林一定不会过拟合 |
| 下游 | Boosting | 逐步拟合梯度/残差以构造加法模型 | 验证集上无限早停/调参 |

现在不用背表。请把三行连成一句因果解释，再用 **Bagging 与随机森林为什么降方差** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“许多相关的树平均以后，误差会在什么条件下下降？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“决策树如何切开特征空间”提供前置条件，本章用 **Bagging** 解释关键机制，并把结果留在“树间相关、袋外误差与方差对照”中。下一主题“Boosting：沿着损失梯度加树”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“只比较默认线性模型与精调 boosting”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“模型选择使用预算相同的嵌套验证而非各跑默认值”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **树间相关、袋外误差与方差对照**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 决策树 怎样经过 Bagging 影响结果

单树选择使纯度或损失下降最大的切分，并在叶节点输出均值/概率。随机森林通过样本与特征随机性降低树间相关，从而降低方差。梯度提升维护加法模型 $F_m$，每轮用新树拟合当前损失对预测的负梯度；学习率、树深、叶样本和轮数共同控制容量。CatBoost 的有序目标统计用于降低类别目标编码泄漏。

~~~~mermaid
flowchart LR
  D[表格数据] --> T[单树分区]
  T --> B{集成方式}
  B -->|重采样并行| RF[Random Forest]
  B -->|拟合梯度串行| GB[Boosting]
  RF --> E[平均/投票]
  GB --> E
  E --> C[校准/切片/延迟]
  C --> G[GBDT 与其他基线门禁]
~~~~

读图时从结果逆向追问：哪一步能够观察，哪一步只是假设，哪一步失效后必须让系统拒绝或降级。

## 把“Bagging 与随机森林为什么降方差”做成可重放实验

实验标题：**树间相关、袋外误差与方差对照**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“许多相关的树平均以后，误差会在什么条件下下降？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 验证集上无限早停/调参。

### 2. 执行本章关键动作

比较单树、随机森林、XGBoost/CatBoost 和线性模型

- **必须保存：** 校准、方差、置信区间、最坏切片。
- **停止信号：** 用 impurity importance 声称因果重要。

### 3. 主动注入反例

构造“验证集上无限早停/调参”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 只比较默认线性模型与精调 boosting。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 树间相关、袋外误差与方差对照及其适用范围。
- **停止信号：** 忽略树模型概率同样可能失准。

## 两份证据把“Bagging 与随机森林为什么降方差”的边界画在哪里

### XGBoost: A Scalable Tree Boosting System（2016-06-10）

本章用它校准“Bagging 与随机森林为什么降方差”的边界：贡献是把 boosting 的目标、缺失方向和工程并行结合成可复用系统。。**不要越过的边界：**原论文年代较早；现代 CatBoost、LightGBM 和表格基础模型需要在同一预算下重测。

### Beyond IID: How General Are Tabular Foundation Models, Really?（2026-06-29）

本章用它校准“Bagging 与随机森林为什么降方差”的边界：贡献是把基准从基础模型擅长的区域扩到真实困难区域。。**不要越过的边界：**预印本和统一框架可能无法包含每个模型的最优领域调参；排名不能替代本地任务对照。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| Beyond IID: How General Are Tabular Foundation Models, Really? | 带着“许多相关的树平均以后，误差会在什么条件下下降？”阅读：BeyondArena 汇集 IID、时间、群组、多特征类型与规模变化的 142 个数据集，比较 11 个模型，并配套 Data Foundry 统一元数据和运行协议。 | 预印本和统一框架可能无法包含每个模型的最优领域调参；排名不能替代本地任务对照。 |

建议先读教材或标准中与 **Bagging 与随机森林为什么降方差** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：Random Forests、XGBoost、CatBoost。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 当 Bagging 失效，先查哪里

先排除这些看似合理的误解：树不需要任何数据治理；随机森林一定不会过拟合；boosting 轮数越多越好；SHAP 等于因果贡献；最新基础模型已经淘汰 GBDT。

### 验证集上无限早停/调参

先查看 **可靠性**：校准、方差、置信区间、最坏切片。如果证据确认失败，回到“模型选择使用预算相同的嵌套验证而非各跑默认值”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 只比较默认线性模型与精调 boosting

先查看 **运维**：漂移、缺失、延迟、人工接管和回滚。如果证据确认失败，回到“解释性结论必须附相关特征和扰动稳定性”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “树间相关、袋外误差与方差对照”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 模型选择使用预算相同的嵌套验证而非各跑默认值 | 校准、方差、置信区间、最坏切片 | 验证集上无限早停/调参 |
| 类别编码、缺失方向和单调约束进入模型合同 | 训练/推理时间、内存、能耗或费用 | 用 impurity importance 声称因果重要 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“树间相关、袋外误差与方差对照”，否则下一次改动只能重新争论。

## 把“Bagging 与随机森林为什么降方差”迁移到新条件

围绕“Bagging 与随机森林为什么降方差”完成如下变式：在一个包含相关特征的数据集上训练 boosting，分别计算 gain、permutation 和 SHAP；删除或复制一个强相关变量后观察排名变化，写出不能从重要性推出的结论。

提交“树间相关、袋外误差与方差对照”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“Bagging 与随机森林为什么降方差”：许多相关的树平均以后，误差会在什么条件下下降？ 预期产物是树间相关、袋外误差与方差对照。
请为这份表格任务设计线性、单树、随机森林、XGBoost/CatBoost 的公平对照。固定切分与总搜索预算，说明缺失和类别处理；报告区分、校准、关键切片、训练/推理资源；用 permutation 和相关特征反例审计重要性，不把 SHAP 写成因果。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="学习“Bagging 与随机森林为什么降方差”时，哪种做法最不可信？" :options='["随机森林一定不会过拟合","完成树间相关、袋外误差与方差对照并保留反例","先冻结任务和评价口径"]' :answer="0" explanation="“随机森林一定不会过拟合”忽略了本章的机制或边界。" />

## 本章小结

- **问题：** 许多相关的树平均以后，误差会在什么条件下下降？
- **机制：** 树模型强在对表格数据的缺失、非线性和交互具有合适归纳偏置，但特征重要性、概率和分布外行为仍需单独验证。
- **产物：** 树间相关、袋外误差与方差对照
- **边界：** 特征重要性无法回答的因果问题

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释bagging 与 boosting 的误差机制，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成树间相关、袋外误差与方差对照，保留失败样本，并说明特征重要性无法回答的因果问题。</p>
</div>

<EvidenceTracker lesson="field-machine-learning-14-bagging-random-forest" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/1603.02754">XGBoost</a>、<a href="https://arxiv.org/abs/1706.09516">CatBoost</a>、<a href="https://arxiv.org/abs/2606.30410">BeyondArena</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
