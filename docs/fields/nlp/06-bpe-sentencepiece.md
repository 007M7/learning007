# 06 · BPE 与 SentencePiece 如何学词表

> 频次合并怎样在词表大小和序列长度间取舍？

<div class="lesson-meta"><span>NLPF06</span><span>阶段一 · 语言、语料与切分</span><span>原理推导课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>手算 BPE 与词表实验。</p>
  <p><strong>最低前置：</strong>完成 NLPF01—03，并能按语种、书写系统和文本来源切片。</p>
</div>

## 为什么现在要学“BPE 与 SentencePiece 如何学词表”

公式不是装饰。它的价值是迫使我们写清对象、假设和优化目标，再检查现实是否满足。

**本章核心判断：** BPE 以频繁符号合并压缩序列，词表越大未必越好，因为稀有组合和跨域变化仍存在。

本章只追一个判断：**频次合并怎样在词表大小和序列长度间取舍？** 最后必须用“手算 BPE 与词表实验”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 用公式审查“BPE 与 SentencePiece 如何学词表”

BPE 的贪心目标可写为选择 $a,b$ 使频数 $f(ab)$ 最大；Unigram 近似最大化 $\sum_x\log\sum_{s\in S(x)}\prod_{t\in s}p(t)$。跨语言审计至少报告膨胀率 $r_l=\frac{tokens_l}{characters_l}$、截断率和单位语义成本 $C_l=price\cdot tokens_l/task$。

逐个把符号放回真实任务，并检查：

- Unicode 归一化和空白规则是否改变实体
- 训练词表的语言/领域配比
- 长度比较使用字符、字节还是语义对齐段落
- 特殊 token、注入和反序列化是否安全

推导完成不代表结论成立。至少改变一次关键条件，观察 **形态与词汇** 的结论是否保持；若不保持，把条件写入“手算 BPE 与词表实验”而不是藏在代码默认值里。

## 给“BPE 与 SentencePiece 如何学词表”一个可操作定义

BPE 以频繁符号合并压缩序列，词表越大未必越好，因为稀有组合和跨域变化仍存在。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **手算 BPE 与词表实验**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 切分单位 | 字符、字节、词或子词决定模型看到的基本序列 | 只报告平均 token 长度 |
| 当前支点 | 形态与词汇 | 是否需要形态分析或领域词保护 | 词表越大越好 |
| 下游 | Tokenizer 公平性 | 同一语义在不同语言上的长度、成本和截断概率不同 | 把中文字符直接当词 |

现在不用背表。请把三行连成一句因果解释，再用 **BPE 与 SentencePiece 如何学词表** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“频次合并怎样在词表大小和序列长度间取舍？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“词、词素与文字系统”提供前置条件，本章用 **形态与词汇** 解释关键机制，并把结果留在“手算 BPE 与词表实验”中。下一主题“Tokenizer 的成本与语言公平”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“忽略 offset 对抽取高亮的影响”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“缓存同时保存原文、字符偏移与 token 偏移”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **手算 BPE 与词表实验**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 切分单位 怎样经过 形态与词汇 影响结果

BPE 每轮合并频率最高的相邻符号；Unigram 从候选子词集合中选择最大化语料似然的子集；byte 模型彻底消除 OOV，却显著拉长序列。BLT 不固定 token 表，而按字节熵形成动态 patch：可预测片段用大块，稀有/复杂片段用小块，从而把计算分配给信息密集区域。

~~~~mermaid
flowchart LR
  X[Unicode 文本] --> N[规范化]
  N --> U{编码方案}
  U -->|BPE/Unigram| T[固定子词]
  U -->|Byte| B[字节流]
  B --> P[熵驱动 patch]
  T --> L[长度/覆盖/成本]
  P --> L
  L --> E[跨语言任务评测]
~~~~

读图时从结果逆向追问：哪一步能够观察，哪一步只是假设，哪一步失效后必须让系统拒绝或降级。

## 把“BPE 与 SentencePiece 如何学词表”做成可重放实验

实验标题：**手算 BPE 与词表实验**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“频次合并怎样在词表大小和序列长度间取舍？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 把中文字符直接当词。

### 2. 执行本章关键动作

按语种/文体计算 token 膨胀、实体破碎和截断

- **必须保存：** 端到端时延、成功率、降级/拒绝和人工介入。
- **停止信号：** 训练测试使用不同规范化。

### 3. 主动注入反例

构造“把中文字符直接当词”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 忽略 offset 对抽取高亮的影响。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 手算 BPE 与词表实验及其适用范围。
- **停止信号：** 认为无 OOV 就没有语言偏差。

## 两份证据把“BPE 与 SentencePiece 如何学词表”的边界画在哪里

### SentencePiece（2018-08-21）

本章用它校准“BPE 与 SentencePiece 如何学词表”的边界：建立了现代多语言 tokenizer 的可复现基础。。**不要越过的边界：**语言无关实现不等于语言公平；训练混合和词表容量仍决定分配。

### Byte Latent Transformer（2024-12-13）

本章用它校准“BPE 与 SentencePiece 如何学词表”的边界：提供按信息复杂度而非固定词表分配计算的路线。。**不要越过的边界：**结果来自特定规模与训练配方；推理栈、缓存和真实多语言成本仍需独立测量。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| Byte Latent Transformer | 带着“频次合并怎样在词表大小和序列长度间取舍？”阅读：BLT 在字节层建模，以熵阈值形成动态 patch，并在局部 byte 编码器与全局 latent Transformer 之间传递表示。 | 结果来自特定规模与训练配方；推理栈、缓存和真实多语言成本仍需独立测量。 |

建议先读教材或标准中与 **BPE 与 SentencePiece 如何学词表** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：BPE、SentencePiece、ByT5。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 当 形态与词汇 失效，先查哪里

先排除这些看似合理的误解：Tokenizer 只是工程预处理；词表越大越好；无 OOV 等于跨语言公平；更少 token 必然更强；换 tokenizer 不影响模型接口。

### 把中文字符直接当词

先查看 **系统表现**：端到端时延、成功率、降级/拒绝和人工介入。如果证据确认失败，回到“缓存同时保存原文、字符偏移与 token 偏移”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 忽略 offset 对抽取高亮的影响

先查看 **风险运维**：漂移、越权、事故、恢复时间和残余风险。如果证据确认失败，回到“Tokenizer 更新视为数据接口破坏性变更”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “手算 BPE 与词表实验”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 缓存同时保存原文、字符偏移与 token 偏移 | 端到端时延、成功率、降级/拒绝和人工介入 | 把中文字符直接当词 |
| 费用/延迟看每项任务而非每 token 单价 | 训练/推理/标注成本、吞吐、容量与能耗 | 训练测试使用不同规范化 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“手算 BPE 与词表实验”，否则下一次改动只能重新争论。

## 把“BPE 与 SentencePiece 如何学词表”迁移到新条件

围绕“BPE 与 SentencePiece 如何学词表”完成如下变式：选择三种语言和一种领域文体，对同一批语义对齐文本比较至少两个 tokenizer；提交分布图、最差样本、下游抽取结果与一条词表/上下文决策。

提交“手算 BPE 与词表实验”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“BPE 与 SentencePiece 如何学词表”：频次合并怎样在词表大小和序列长度间取舍？ 预期产物是手算 BPE 与词表实验。
对给定多语言语料做 tokenizer 审计：输出每语种 token/字符、token/字节、P95、截断率、实体跨片率、噪声鲁棒性、任务质量和单位成本；列出最差 20 条并解释机制。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="学习“BPE 与 SentencePiece 如何学词表”时，哪种做法最不可信？" :options='["词表越大越好","完成手算 BPE 与词表实验并保留反例","先冻结任务和评价口径"]' :answer="0" explanation="“词表越大越好”忽略了本章的机制或边界。" />

## 本章小结

- **问题：** 频次合并怎样在词表大小和序列长度间取舍？
- **机制：** Tokenizer 是数据压缩器、计算分配器和语言接口；它不会只影响输入长度，而会改变上下文预算、训练频率、成本与错误形态。
- **产物：** 手算 BPE 与词表实验
- **边界：** 固定子词方案会系统性失效的输入

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释BPE、Unigram 与动态 byte patch 的权衡，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成手算 BPE 与词表实验，保留失败样本，并说明固定子词方案会系统性失效的输入。</p>
</div>

<EvidenceTracker lesson="field-nlp-06-bpe-sentencepiece" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://aclanthology.org/D18-2012/">SentencePiece</a>、<a href="https://arxiv.org/abs/2412.09871">BLT</a>、<a href="https://aclanthology.org/P16-1162/">BPE NMT</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
