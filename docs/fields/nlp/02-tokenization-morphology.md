# 02 · 分词、形态与开放词汇

> Tokenizer 是数据压缩器、计算分配器和语言接口；它不会只影响输入长度，而会改变上下文预算、训练频率、成本与错误形态。

<div class="lesson-meta"><span>NLPF04—NLPF05—NLPF06</span><span>核心主线</span><span>预计 6 × 45 分钟</span><span>更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>现在学它，需要什么：</strong>完成 NLPF01—03，并能按语种、书写系统和文本来源切片。</p>
  <p><strong>学完要能做到：</strong>解释字符、字节、词片与形态单位的权衡；量化不同语言的 token 膨胀和截断偏差；在开放词汇、成本与下游质量之间做选择。</p>
</div>

## 这次只练一件事：比较子词与字节表示

同一份跨境合同，英文 800 tokens、中文 1,100、某低资源语言 2,400。固定 2,048 上下文会让后者更早丢失签名条款，按 token 计费还提高成本。只比较英文下游分数无法发现这类结构性差异。

不要急着把整套系统做完。本章先用一个可重放的小任务回答：**Tokenizer 为什么会系统性改变不同语言的成本与能力？**

## 示范任务：中英少数语种合同 tokenizer 审计

构造语义对齐的 1,000 段合同，分别统计字符、字节、token、截断位置和实体破碎率。固定语义内容与模型，比较子词和 byte/patch 方案在条款检索、金额抽取、吞吐和费用上的 Pareto；额外加入拼写噪声、混语和罕见公司名。

### 读案例时做三次停顿

1. 到 切分单位 时停一次：何种单位保留结构且可覆盖新词？
2. 到 形态与词汇 时再停一次：哪条数据流、状态变化或约束能证明机制真的发生？
3. 到 Tokenizer 公平性 时最后停一次：如果出现“只报告平均 token 长度”，系统应继续、拒绝还是交给人？

## 开放词汇编码与动态 byte patch

BPE 每轮合并频率最高的相邻符号；Unigram 从候选子词集合中选择最大化语料似然的子集；byte 模型彻底消除 OOV，却显著拉长序列。BLT 不固定 token 表，而按字节熵形成动态 patch：可预测片段用大块，稀有/复杂片段用小块，从而把计算分配给信息密集区域。

~~~~mermaid
flowchart LR
  X[Unicode 文本] --> N[规范化]
  N --> U{编码方案}
  U -->|BPE/Unigram| T[固定子词]
  U -->|Byte| B[字节流]
  B --> P[熵驱动 patch]
  T --> L[长度/覆盖/成本]
  P --> L
  L --> E[跨语言任务评测]
~~~~

## 跟我走一遍：比较子词与字节表示

### 第 1 步：冻结规范化、词表和模型版本

- **这一步在验证：** 切分单位能否支持“何种单位保留结构且可覆盖新词”这项判断。
- **至少留下：** 主指标、分层指标、置信区间和失败类型。
- **先停下来而不是继续调参的信号：** 只报告平均 token 长度。

### 第 2 步：按语种/文体计算 token 膨胀、实体破碎和截断

- **这一步在验证：** 形态与词汇能否支持“是否需要形态分析或领域词保护”这项判断。
- **至少留下：** 端到端时延、成功率、降级/拒绝和人工介入。
- **先停下来而不是继续调参的信号：** 把中文字符直接当词。

### 第 3 步：在等上下文成本而非等 token 条件评测下游任务

- **这一步在验证：** Tokenizer 公平性能否支持“词表与 patch 策略是否系统性伤害某些语言”这项判断。
- **至少留下：** 训练/推理/标注成本、吞吐、容量与能耗。
- **先停下来而不是继续调参的信号：** 训练测试使用不同规范化。

### 第 4 步：对稀有词、混语、emoji、代码和注入边界做反例集

- **这一步在验证：** 切分单位能否支持“何种单位保留结构且可覆盖新词”这项判断。
- **至少留下：** 漂移、越权、事故、恢复时间和残余风险。
- **先停下来而不是继续调参的信号：** 忽略 offset 对抽取高亮的影响。

<details class="ai-workbench">
<summary>让 AI 扮演挑错的实验搭档</summary>

~~~~text
对给定多语言语料做 tokenizer 审计：输出每语种 token/字符、token/字节、P95、截断率、实体跨片率、噪声鲁棒性、任务质量和单位成本；列出最差 20 条并解释机制。
~~~~

先让 AI 复述任务、证据和禁止外推的范围，再接受它生成的方案。
</details>

## 做到什么，才算结果可信

| 观察维度 | 要留下的记录 | 它防止你误判什么 |
|---|---|---|
| 任务质量 · 切分单位 | 主指标、分层指标、置信区间和失败类型 | 平均分不能说明关键场景是否可用；本章还要据此判断：何种单位保留结构且可覆盖新词 |
| 系统表现 · 形态与词汇 | 端到端时延、成功率、降级/拒绝和人工介入 | 局部模型分数不是用户任务结果；本章还要据此判断：是否需要形态分析或领域词保护 |
| 资源经济 · Tokenizer 公平性 | 训练/推理/标注成本、吞吐、容量与能耗 | 确认改进在真实预算下仍成立；本章还要据此判断：词表与 patch 策略是否系统性伤害某些语言 |
| 风险运维 · 切分单位 | 漂移、越权、事故、恢复时间和残余风险 | 把一次实验转化成可持续服务；本章还要据此判断：何种单位保留结构且可覆盖新词 |

## 为什么这些步骤能够解释结果

BPE 的贪心目标可写为选择 $a,b$ 使频数 $f(ab)$ 最大；Unigram 近似最大化 $\sum_x\log\sum_{s\in S(x)}\prod_{t\in s}p(t)$。跨语言审计至少报告膨胀率 $r_l=\frac{tokens_l}{characters_l}$、截断率和单位语义成本 $C_l=price\cdot tokens_l/task$。

把每个符号放回真实系统。下面任何一项说不清，计算正确也不能保证结论正确：

- Unicode 归一化和空白规则是否改变实体
- 训练词表的语言/领域配比
- 长度比较使用字符、字节还是语义对齐段落
- 特殊 token、注入和反序列化是否安全

## 拿公开证据校准你的实验

不必复述整篇论文，但要检查自己的任务单位、基线、预算和运行边界是否与来源一致。

### SentencePiece（2018-08-21）

先看这份来源时，要带着一个具体问题：能否直接从原始句子学习语言无关的子词模型？

SentencePiece 将空格当普通符号，在固定词表下提供 Unigram/BPE 和可逆切分，避免依赖语言特定预分词。 论文在日英翻译等设置展示与传统子词管线相当或更好的结果，并简化多语言处理。 因此，本章采用它来支持这样的判断：建立了现代多语言 tokenizer 的可复现基础。

但它没有替我们回答所有问题。语言无关实现不等于语言公平；训练混合和词表容量仍决定分配。

### Byte Latent Transformer（2024-12-13）

最后看这份来源时，要带着一个具体问题：固定 token 是否是扩展语言模型的必要前提？

BLT 在字节层建模，以熵阈值形成动态 patch，并在局部 byte 编码器与全局 latent Transformer 之间传递表示。 作者报告在相近训练 FLOPs 下可匹配 token 模型，并在噪声和字符任务上表现出优势。 因此，本章采用它来支持这样的判断：提供按信息复杂度而非固定词表分配计算的路线。

但它没有替我们回答所有问题。结果来自特定规模与训练配方；推理栈、缓存和真实多语言成本仍需独立测量。

## 轮到你：换一组条件再做：比较子词与字节表示

选择三种语言和一种领域文体，对同一批语义对齐文本比较至少两个 tokenizer；提交分布图、最差样本、下游抽取结果与一条词表/上下文决策。

提交时一并回答：

- 你能否不看正文解释BPE、Unigram 与动态 byte patch 的权衡；
- 产物是否真的包含一个三语 tokenizer 审计报告；
- 哪个失败样本暴露了固定子词方案会系统性失效的输入；
- 如果换一个数据、人群、环境或本体，最先需要重测哪项指标。

提交物不能只有成功截图。附上一个失败样本，并说明它推翻了什么假设、哪些残余风险还不能消除。

## 验证通过后，项目要改变什么

### 1. 上下文窗口按最差语言预算

验收时查看 **任务质量**，至少保存主指标、分层指标、置信区间和失败类型。如果观察到“只报告平均 token 长度”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 2. 缓存同时保存原文、字符偏移与 token 偏移

验收时查看 **系统表现**，至少保存端到端时延、成功率、降级/拒绝和人工介入。如果观察到“把中文字符直接当词”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 3. 费用/延迟看每项任务而非每 token 单价

验收时查看 **资源经济**，至少保存训练/推理/标注成本、吞吐、容量与能耗。如果观察到“训练测试使用不同规范化”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 4. Tokenizer 更新视为数据接口破坏性变更

验收时查看 **风险运维**，至少保存漂移、越权、事故、恢复时间和残余风险。如果观察到“忽略 offset 对抽取高亮的影响”，这项决定必须重新评审，不能因为已经实现就默认保留。

## 卡住时先查这几处

先排除这些误解：Tokenizer 只是工程预处理；词表越大越好；无 OOV 等于跨语言公平；更少 token 必然更强；换 tokenizer 不影响模型接口。

### 只报告平均 token 长度

- **先看什么：** 主指标、分层指标、置信区间和失败类型
- **回到哪项控制：** 上下文窗口按最差语言预算
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“比较子词与字节表示”的回归记录。

### 把中文字符直接当词

- **先看什么：** 端到端时延、成功率、降级/拒绝和人工介入
- **回到哪项控制：** 缓存同时保存原文、字符偏移与 token 偏移
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“比较子词与字节表示”的回归记录。

### 训练测试使用不同规范化

- **先看什么：** 训练/推理/标注成本、吞吐、容量与能耗
- **回到哪项控制：** 费用/延迟看每项任务而非每 token 单价
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“比较子词与字节表示”的回归记录。

### 忽略 offset 对抽取高亮的影响

- **先看什么：** 漂移、越权、事故、恢复时间和残余风险
- **回到哪项控制：** Tokenizer 更新视为数据接口破坏性变更
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“比较子词与字节表示”的回归记录。

### 认为无 OOV 就没有语言偏差

- **先看什么：** 主指标、分层指标、置信区间和失败类型
- **回到哪项控制：** 上下文窗口按最差语言预算
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“比较子词与字节表示”的回归记录。

## 🎯 随堂检验

<Quiz question="某语种答案常在长文末尾被漏掉，首先检查什么？" :options='["温度","该语种 token 膨胀与实际截断位置","增加英文训练集"]' :answer="1" explanation="相同字符/语义长度可能消耗不同上下文，先验证表示与截断机制。" />

## 本章小结

- 切分决定序列和计算预算
- 形态与书写系统改变最优粒度
- 公平审计需要语义对齐与下游任务
- Tokenizer 版本属于模型契约

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释BPE、Unigram 与动态 byte patch 的权衡，并完成一个三语 tokenizer 审计报告。</p>
  <p><strong>真正会用：</strong>换一个场景仍能指出固定子词方案会系统性失效的输入，同时保留失败样本或拒绝条件。</p>
</div>

<EvidenceTracker lesson="field-nlp-02-tokenization-morphology" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://aclanthology.org/D18-2012/">SentencePiece</a>、<a href="https://arxiv.org/abs/2412.09871">BLT</a>、<a href="https://aclanthology.org/P16-1162/">BPE NMT</a>。涉及新模型、法规或标准时，请按链接中的版本和适用范围复核。</div>
