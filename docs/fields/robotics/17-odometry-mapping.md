# 17 · 里程计、地图与漂移

> 局部运动估计为何必然累积误差？

<div class="lesson-meta"><span>RBF17</span><span>阶段三 · 导航与操作</span><span>机制拆解课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>轨迹误差与漂移来源图。</p>
  <p><strong>最低前置：</strong>完成三维状态估计和反馈控制。</p>
</div>

## 为什么现在要学“里程计、地图与漂移”

你看到的是输出变化，真正需要追的是状态、信息和约束怎样传递。仓库机器人平均可达率高，但长走廊感知退化后错位，局部规划器在动态叉车旁振荡。

**本章核心判断：** 里程计积分局部运动必然漂移，地图与外部观测负责提供长期约束。

本章只追一个判断：**局部运动估计为何必然累积误差？** 最后必须用“轨迹误差与漂移来源图”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 把“里程计、地图与漂移”放回 40 章依赖链

~~~~mermaid
flowchart LR
  A[上一主题：机器人需要 Belief，不只需要状态值] -->|提供前置| B[当前：里程计、地图与漂移]
  B -->|留下：轨迹误差与漂移来源图| C[下一主题：回环检测与位姿图优化]
  C --> D{能否支持：轨迹误差与漂移来源图}
  D -->|不能| E[回到假设与数据]
  D -->|能够| F[进入下一主题]
~~~~

这张图不是通用真理，而是本章的待验证模型。导航成功率是感知、估计、地图、规划、控制和恢复的乘积；单次无碰到达掩盖定位漂移与地图污染。 如果输入、环境或责任边界改变，箭头也要重新检查。

## 给“里程计、地图与漂移”一个可操作定义

里程计积分局部运动必然漂移，地图与外部观测负责提供长期约束。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **轨迹误差与漂移来源图**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 恢复与主动 SLAM | 系统主动获取信息并从定位/规划失败恢复 | 一次建图永久使用 |
| 当前支点 | SLAM | 地图是否一致且可复定位 | ATE 低等于导航好 |
| 下游 | 导航栈 | 全局路径、局部避障和控制闭环协同 | 只报 ATE 不报任务 |

现在不用背表。请把三行连成一句因果解释，再用 **里程计、地图与漂移** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## “局部运动估计为何必然累积误差？”依赖哪些前提

本章暂时不展开整套推导，但必须把判断拆成可以检查的前提：

| 顺序 | 必须成立的前提 | 对应控制 |
|---|---|---|
| 1 | 回环假阳性代价；在“里程计、地图与漂移”中必须显式核对 | 地图有版本/适用时段 |
| 2 | 动态对象是否进入静态地图；在“里程计、地图与漂移”中必须显式核对 | 动态障碍不永久写静态图 |
| 3 | 里程计/地图 frame 语义；在“里程计、地图与漂移”中必须显式核对 | 恢复次数/距离设上限 |
| 4 | 重置间任务是否独立；在“里程计、地图与漂移”中必须显式核对 | 定位不确定超阈值降速/停止 |

这四项共同约束“局部运动估计为何必然累积误差？”。任何一项只能靠猜测回答时，都应降低结论强度或补实验。

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“局部运动估计为何必然累积误差？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“机器人需要 Belief，不只需要状态值”提供前置条件，本章用 **SLAM** 解释关键机制，并把结果留在“轨迹误差与漂移来源图”中。下一主题“回环检测与位姿图优化”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“每次失败手工重启不计”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“地图有版本/适用时段”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **轨迹误差与漂移来源图**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 把“里程计、地图与漂移”做成可重放实验

实验标题：**轨迹误差与漂移来源图**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“局部运动估计为何必然累积误差？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 一次建图永久使用。

### 2. 执行本章关键动作

建立真值/回放与模块 trace

- **必须保存：** 成功率、完成时间、部分进展与恢复。
- **停止信号：** 只报 ATE 不报任务。

### 3. 主动注入反例

构造“一次建图永久使用”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 每次失败手工重启不计。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 轨迹误差与漂移来源图及其适用范围。
- **停止信号：** 回环假阳性无检测。

## 两份证据把“里程计、地图与漂移”的边界画在哪里

### ORB-SLAM3（2021-12-13）

这项工作追问：视觉/惯性 SLAM 如何覆盖多地图和重定位？

系统整合单目/双目/RGB-D 与视觉惯性，并支持 Atlas 多地图。 论文在多数据集展示强精度与鲁棒性。

本章据此采用的判断是：**提供现代开源 SLAM 基线。** 但不能外推到：公开数据不覆盖所有仓库动态/无纹理/长期变化，需本地复测。

### Nav2 Documentation（2026-08-30）

本章用它校准“里程计、地图与漂移”的边界：适合做分层消融。。**不要越过的边界：**配置示例不是产品安全证明，实时/硬件/人机风险需另验。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| ORB-SLAM3 | 带着“局部运动估计为何必然累积误差？”阅读：系统整合单目/双目/RGB-D 与视觉惯性，并支持 Atlas 多地图。 | 公开数据不覆盖所有仓库动态/无纹理/长期变化，需本地复测。 |

建议先读教材或标准中与 **里程计、地图与漂移** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：Probabilistic Robotics、ORB-SLAM3、Nav2。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 当 SLAM 失效，先查哪里

先排除这些看似合理的误解：ATE 低等于导航好；地图越精细越好；重规划可解决定位错误；动态物体应写入静态地图；成功到达即可忽略近失。

### 一次建图永久使用

先查看 **任务**：成功率、完成时间、部分进展与恢复。如果证据确认失败，回到“地图有版本/适用时段”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 每次失败手工重启不计

先查看 **泛化**：对象、场景、语言、本体和扰动切片。如果证据确认失败，回到“恢复次数/距离设上限”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “轨迹误差与漂移来源图”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 地图有版本/适用时段 | 成功率、完成时间、部分进展与恢复 | 一次建图永久使用 |
| 动态障碍不永久写静态图 | 碰撞/力/间隔/约束违例和人工接管 | 只报 ATE 不报任务 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“轨迹误差与漂移来源图”，否则下一次改动只能重新争论。

## 把“里程计、地图与漂移”迁移到新条件

围绕“里程计、地图与漂移”完成如下变式：在仿真设 10 对起终点，每对 5 次，加入动态障碍；提交 SPL、碰撞、恢复和按层失败漏斗。

提交“轨迹误差与漂移来源图”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“里程计、地图与漂移”：局部运动估计为何必然累积误差？ 预期产物是轨迹误差与漂移来源图。
设计导航分层评测：SLAM/定位、地图、全局/局部规划、控制和恢复；用 oracle 组件消融，注入回环假阳性、动态障碍、轮滑和遮挡，报告任务与过程指标。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="Oracle 定位后导航显著变好，主要瓶颈在哪层？" :options='["UI","SLAM/定位而非规划策略本身","语音"]' :answer="1" explanation="替换实验隔离了状态估计误差。" />

## 本章小结

- **问题：** 局部运动估计为何必然累积误差？
- **机制：** 导航成功率是感知、估计、地图、规划、控制和恢复的乘积；单次无碰到达掩盖定位漂移与地图污染。
- **产物：** 轨迹误差与漂移来源图
- **边界：** 需要人工求助的恢复上限

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释地图/定位误差怎样传到控制，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成轨迹误差与漂移来源图，保留失败样本，并说明需要人工求助的恢复上限。</p>
</div>

<EvidenceTracker lesson="field-robotics-17-odometry-mapping" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/2007.11898">ORB-SLAM3</a>、<a href="https://docs.nav2.org/">Nav2</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
