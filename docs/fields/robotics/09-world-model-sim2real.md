# 09 · 世界模型、合成数据与 Sim2Real

> 一句话点题：生成视频像真实不等于世界模型在物理上正确；控制需要动作条件、三维/接触一致、长期误差和不确定度。

<div class="lesson-meta"><span>RBF25—RBF26—RBF27</span><span>核心主线</span><span>预计 6 × 45 分钟</span><span>证据截止 2026-08-30</span></div>

## 解锁与跳过

掌握动力学、VLA 和可靠实验设计。 即使你使用 AI 完成实现，也不能跳过本章的变量定义、反例和评测设计；这些部分决定你是否有能力发现一个“运行正常但结论错误”的系统。

## 本章可观察目标

完成后你能：区分视觉生成、三维一致和可控动力学模型；用世界模型做预测/规划并审计误差累积；设计 Sim2Real 随机化、系统辨识和真实回放。阅读完成不计掌握；至少需要一次不看答案的解释、一次实际应用和一次边界判断。

## 研究问题：视觉逼真的生成世界何时仍会在物理上欺骗策略？

模型生成机器人稳定搬箱视频，但箱子质量、遮挡后位置和碰撞反应不守恒。策略在该世界规划得到的成功是模拟器漏洞。必须用 action intervention、几何/动力学探针和真实回放校准。

问题必须被写成“输入、状态、动作/输出、损失、约束、证据和停止条件”的组合。只写“提升效果”会把数据、算法、交互与业务结果混在一起，也无法设计反事实或消融。

## 三个知识节点怎样连接

| 节点 | 核心对象 | 最小充分解释 | 必须支持的决策 |
|---|---|---|---|
| RBF25 | 世界模型 | 从历史与动作预测未来观测/状态分布 | 预测是否保留控制相关因果 |
| RBF26 | 合成数据/仿真 | 渲染与物理引擎扩展场景覆盖 | 仿真参数支持真实分布吗 |
| RBF27 | Sim2Real | 随机化、适配与系统辨识缩小现实差距 | 真实闭环证据何时足够 |

三者不是并列名词：第一个节点通常建立对象和假设，第二个节点解释机制，第三个节点把机制放进测量或交付边界。缺任何一层，都容易把局部指标误当成系统结论。

## 核心机制：动作条件预测与现实差距

世界模型编码状态，给动作预测未来 latent/视频/3D；模型预测控制在滚动时域采样动作并用模型评价。PAIWorld 强调多视角 3D 一致；ABot-PhysWorld 从主动机器人数据批评/训练物理世界；U0 尝试统一视频与动作。合成数据用于覆盖，真实数据校准参数和失败。

理解机制时要区分三种量：**被设计者直接控制的变量**、**环境或数据生成过程决定的变量**、**只能通过代理指标观测的潜变量**。可靠实验一次只改变主要因子，其余配置进入版本化运行记录。

## 关键公式、假设与推导

学习 $p_\theta(s_{t+1}|s_t,a_t)$，规划 $a^*=\arg\max E_\theta\sum R(s_t,a_t)$。多步误差会复合；报告 one-step 与 rollout、几何重投影、碰撞/物体持久、action sensitivity 和真实闭环 regret。

公式不是装饰。逐项检查：

- 数据中动作是否有充分激励
- 模型是否区分相关和干预
- 接触/遮挡/长时记忆
- 仿真参数后验与域随机化范围

若假设不成立，正确做法不是继续套公式，而是换估计量、改采样/实验设计、缩小结论或明确拒绝自动决策。

## 证据拆解1：PAIWorld

### 研究问题

世界模型怎样保持多视角三维一致？

### 核心机制、实验与指标

工作以多视角、3D 感知架构/数据推动可交互世界预测。

论文报告相对视频模型更强几何一致与场景生成。

### 真正贡献、局限与适用边界

代表 2026 3D-aware 世界模型方向。 预印本结果和视觉指标不等于机器人控制有效。

## 证据拆解2：ABot-PhysWorld

### 研究问题

互联网视频足够学习机器人所需物理吗？

### 核心机制、实验与指标

工作强调主动机器人交互数据和物理评测，分析现有世界模型在接触/动作因果上的缺口。

结果支持从被动观看转向主动数据。

### 真正贡献、局限与适用边界

为物理一致性建立反证。 预印本与特定平台；需独立复现。

## 证据拆解3：Xiaomi Robotics U0

### 研究问题

视频世界模型与通用机器人策略能否统一？

### 核心机制、实验与指标

U0 联合多源视频/机器人数据和动作生成。

作者报告相对 π0.5 的 OOD 成功从 36.9% 到 63.2% 等结果。

### 真正贡献、局限与适用边界

代表 2026 联合世界—动作路线。 公司预印本数字绑定其任务/数据，不能视为普适结论。

## 从论文或标准到产品主张：证据审计

| 日期 | 一手来源 | 它真正回答的问题 | 不能据此外推什么 |
|---|---|---|---|
| 2026-06-16 | PAIWorld | 世界模型怎样保持多视角三维一致？ | 预印本结果和视觉指标不等于机器人控制有效。 |
| 2026-03-24 | ABot-PhysWorld | 互联网视频足够学习机器人所需物理吗？ | 预印本与特定平台；需独立复现。 |
| 2026-07-15 | Xiaomi Robotics U0 | 视频世界模型与通用机器人策略能否统一？ | 公司预印本数字绑定其任务/数据，不能视为普适结论。 |

阅读来源时按四步记录：先抄下研究对象、比较基线、数据/场景和预算，再定位结果表或正式条文；随后区分作者直接观察、由结果支持的解释和你准备迁移到项目的推论；最后为推论写一个能推翻它的反例。**来源新并不等于证据强**：预印本、公司技术报告、正式标准、法规和独立复现承担的证明责任不同。数字必须连同分母、置信区间、硬件/本体、语言/人群与失败样本一起保存。

如果来源只证明组件指标改善，不得自动升级成端到端任务、真实用户价值、物理安全或合规结论。迁移到自己的项目时至少保留一个原方法基线、一个更简单基线和一个不使用该能力的基线；结果冲突时，优先检查任务定义、样本污染、资源预算、评价器偏差和运行边界，而不是挑选最符合预期的一项。

## 机制图：从输入到可验证结果

```mermaid
flowchart LR
 D[真实/仿真动作数据] --> W[动作条件世界模型]
 W --> P[滚动预测/规划]
 P --> S[安全壳/执行]
 S --> R[真实结果]
 R --> E[几何/动力学/任务残差]
 E --> C[系统辨识/随机化]
 C --> W
```

图中每条边都应能对应日志、数据版本、物理状态或人工记录；无法观测的边必须标为假设，不允许用模型的一段解释代替真实状态。

## 贯穿案例：推箱接触世界模型审计

构造质量、摩擦、遮挡和多视角矩阵，给相同初态施加不同动作；测位置/姿态、接触时刻、物体持久和 rollout。策略先模型规划，再真实/高保真回放；视觉更好但控制 regret 更差则拒绝。

案例的重点不是跑出最高分，而是保存“为什么选择这条路径”的证据：基线是什么、哪个假设最危险、失败怎样分层、什么条件触发降级或人工接管。

## 复现任务：最小因果链

1. 建立 no-action 与动作干预对
2. 分别测像素/三维/动力学/任务
3. 扫 rollout horizon 和不确定
4. 用真实残差更新仿真参数后复测

复现不是成功运行作者代码。你必须固定数据/环境、预算和评价器，至少重复三次，报告均值、离散程度、失败样本和资源消耗；若只能跑一次，要把结论降级为演示证据。

## 三轮实验与消融路线

**第一轮：测量链校准。** 只运行最简单基线，检查输入单位、时间/坐标/权限、随机种子、日志和评价器。抽取成功与失败样本人工复核；若真值或测量本身不稳定，停止模型比较。输出必须能回答“同一次运行能否被另一人重放”。

**第二轮：机制消融。** 在同一数据、预算、硬件或运行条件下，一次只加入一个关键机制；对每次变化写预期影响、未变化项和失败阈值。除了主指标，还要检查最坏切片、过程约束、P95/P99、资源与人工成本。若提升只在一个方便切片出现，应缩小能力声明。

**第三轮：边界与迁移。** 有计划地改变本章公式假设或 ODD：时间、人群、语言、对象、气象、本体、延迟、权限或供应商至少选择两项；注入缺失、冲突和失效。记录系统是正确拒绝/降级/接管，还是带着错误自信继续。最终产物不是“最佳分数”，而是一个可复核的能力范围、反例集和下一次变更会触发的回归集合。

## 实验与指标

| 维度 | 至少记录什么 | 为什么 |
|---|---|---|
| 任务 | 成功率、完成时间、部分进展与恢复 | 一次漂亮成功不能表示可靠性 |
| 过程 | 碰撞/力/间隔/约束违例和人工接管 | 结果正确也可能过程危险 |
| 泛化 | 对象、场景、语言、本体和扰动切片 | 平均分不说明分布外边界 |
| 系统 | 控制频率、P95、算力、数据/人工和能耗 | 策略必须在真实闭环预算运行 |

同时保留一个简单基线和一个“什么都不做/人工流程”基线。复杂方案只有在质量、成本、时延、风险或可扩展性至少一个维度形成可重复净收益时才晋级。

## 对产品、系统或研究架构的影响

- 世界模型能力按用途声明
- 合成数据带生成配置与血缘
- 规划动作仍经真实安全约束
- 每次 sim 升级重做 reality gap

这些决策应进入 PRD、实验卡、接口契约、安全案例或 ADR，而不只留在学习笔记。模型、数据、提示、硬件、环境与评价器任何一个升级，都要能触发有范围的回归测试。

## 会死在哪里

- FID/视频好看当物理正确
- 训练动作缺少激励
- 多步只评最后帧
- 域随机化范围拍脑袋
- 真实验证挑成功

失败分析要写“触发条件—可观测信号—影响—检测—缓解—残余风险”。只写“模型可能出错”没有工程价值。

## 与 AI 协作模板

```text
审计世界模型：动作干预、多视角3D、对象持久、接触/质量/摩擦、多步误差与不确定；比较视觉指标和控制 regret，设计系统辨识与真实闭环验证。
```

使用模板后仍需检查 AI 是否虚构来源、混用时间口径、悄悄改变任务定义，或把模拟结果外推到真实系统。

## 练习：构造视觉好但物理错的反例

在仿真/视频模型设计同初态不同力的推箱；测轨迹与守恒，写一个会误导规划器的具体反例和检测器。

提交物必须包含一个反例或失败样本。只有成功案例会让你误以为已经掌握；边界证据才能说明你知道方法何时不该用。

## 常见误区

视频逼真等于物理正确；更多互联网视频等于动作因果；仿真随机化越宽越好；单步准则长期准；世界模型可绕过安全控制。共同根因是把一个条件成立时的局部结论，扩张成不带条件的能力判断。

## 自测

<Quiz question="生成视频逼真但对不同推力给相同运动，缺少什么？" :options='["更高分辨率","动作因果敏感性/动力学一致","更多字幕"]' :answer="1" explanation="控制世界模型必须响应动作干预。" />

## 本章小结

- 世界模型为动作条件预测
- 视觉与物理指标分离
- 多步误差会累积
- Sim2Real 由真实残差闭环

<EvidenceTracker lesson="field-robotics-09-world-model-sim2real" />

## 本章完成标准

不看正文解释 视觉真实与控制真实的差异；完成 一个动作干预世界模型评测；能指出 合成证据不能替代的实测。最近相关题平均至少 7/10 才标记为 basic；跨日期完成变式任务并达到 8.5/10，才标记为 proficient。

<div class="source-note">主要来源（证据截止 2026-08-30）：<a href="https://arxiv.org/abs/2606.18375">PAIWorld</a>、<a href="https://arxiv.org/abs/2603.23376">ABot-PhysWorld</a>、<a href="https://arxiv.org/abs/2607.11643">Xiaomi Robotics U0</a>。数字只描述来源中的实验设置；标准、法规与产品能力均按页面版本和适用范围解释。</div>
