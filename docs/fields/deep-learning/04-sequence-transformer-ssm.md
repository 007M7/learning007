# 04 · 序列建模、Transformer 与 SSM

> 序列架构的竞争不是 $O(n^2)$ 与 $O(n)$ 的口号，而是内容检索、状态压缩、硬件利用和真实长度分布的联合问题。

<div class="lesson-meta"><span>DLF10—DLF11—DLF12</span><span>核心主线</span><span>预计 7 × 45 分钟</span><span>更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>现在学它，需要什么：</strong>需掌握矩阵乘法、残差/归一化、序列张量和基本复杂度。</p>
  <p><strong>学完要能做到：</strong>推导自注意力与因果掩码；解释 KV cache、FlashAttention 和长序列瓶颈；比较 Transformer 与选择性 SSM 的质量/吞吐/记忆。</p>
</div>

## 先别问谁最好，先问你在优化什么

> 注意力的二次复杂度是否意味着 Transformer 必然被替代？

标准注意力能让任意 token 直接比较，却为训练构造 $n^2$ 关系；自回归推理又被 KV cache 容量和内存带宽限制。线性 SSM 压缩历史，长序列吞吐更好，但固定状态可能丢失精确回忆。论文中的百万长度不代表每种任务都有效利用百万上下文。

## 把选择拆成三条判断轴

| 知识节点 | 先用一句话说清 | 什么时候值得选择它 | 先排除的误解 |
|---|---|---|---|
| DLF10 · 注意力 | 内容寻址的全局加权聚合 | 任务是否需要任意位置直接交互 | 注意力天然可解释 |
| DLF11 · 高效实现 | 通过分块、融合和缓存减少内存 IO/重复计算 | 瓶颈在 FLOPs、带宽还是容量 | FlashAttention 是近似 |
| DLF12 · 选择性 SSM | 输入依赖状态更新以线性扫描保留/遗忘信息 | 长序列和内容检索如何权衡 | 线性复杂度必然低延迟 |

读表时不要横向背三行。任选一行，把“定义—决定—反例”连成一句因果解释；然后把本章案例中的一个输入替换掉，检查原判断是否仍成立。

## 判断树：内容寻址、IO-aware attention 与选择性状态

Transformer 由 $QK^T$ 得到内容相关权重并聚合 $V$，因果 mask 阻止看未来。FlashAttention 不近似注意力，而是分块把中间矩阵留在片上 SRAM，减少 HBM 读写。Mamba 让 SSM 参数依输入变化，选择保留/忘记 token，并用 hardware-aware scan 并行训练、递归推理。

~~~~mermaid
flowchart LR
  X[Token 序列] --> A[Attention: 全局内容寻址]
  X --> S[SSM: 选择性状态压缩]
  A --> F[Flash/分块 IO]
  A --> K[KV cache 推理]
  S --> H[scan/递归状态]
  F --> E[质量/长度/吞吐]
  K --> E
  H --> E
~~~~

## 不要让一个分数替你做决定

缩放点积注意力：

$$
\operatorname{Attn}(Q,K,V)=\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}+M\right)V.
$$

离散状态空间模型：

$$
h_t=\bar A(x_t)h_{t-1}+\bar B(x_t)x_t,\qquad y_t=C(x_t)h_t.
$$

把每个符号放回真实系统。下面任何一项说不清，计算正确也不能保证结论正确：

- softmax 权重是模型路由而非人类可解释因果
- FlashAttention 改变 IO 复杂度而非数学结果
- KV cache 大小随层、头、长度和 dtype 增长
- SSM 线性时间不保证精确任意位置检索

## 做到什么，才算结果可信

| 观察维度 | 要留下的记录 | 它防止你误判什么 |
|---|---|---|
| 任务质量 · 注意力 | 主指标、切片、置信区间与失败样本 | 确认改进是否稳定且可解释；本章还要据此判断：任务是否需要任意位置直接交互 |
| 训练 · 高效实现 | loss、梯度/激活、吞吐、显存、数据量、FLOPs | 区分算法收益和更多计算；本章还要据此判断：瓶颈在 FLOPs、带宽还是容量 |
| 推理 · 选择性 SSM | 首 Token、每 Token、P50/P95、吞吐、峰值内存 | 模型参数量不等于用户体验；本章还要据此判断：长序列和内容检索如何权衡 |
| 风险 · 注意力 | 校准、偏移、攻击、记忆/污染与能耗 | 把能力放入真实部署边界；本章还要据此判断：任务是否需要任意位置直接交互 |

## 不要只看宣传：两份可核验依据

公开结果可以帮助设置基线，却不能替代你自己的数据、预算和失败代价。

### FlashAttention（2022-06-23）

先看这份来源时，要带着一个具体问题：能否通过 IO-aware 精确算法加速注意力并降低内存？

算法把 $Q,K,V$ 分块，在 SRAM 中在线维护 softmax 统计，避免物化完整注意力矩阵；并给出 HBM IO 分析。 论文报告相对基线显著加速和节省内存，并支持更长上下文。 因此，本章采用它来支持这样的判断：贡献是把硬件内存层级变成算法设计变量。

但它没有替我们回答所有问题。收益依 GPU、序列形状、内核和后续版本；不能把理论 FLOPs 当实际速度。

### Mamba: Linear-Time Sequence Modeling with Selective State Spaces（2023-12-01）

最后看这份来源时，要带着一个具体问题：SSM 如何获得内容相关选择能力并在语言上接近注意力？

Mamba 使状态更新参数依输入变化，使用硬件感知并行 scan；网络不使用注意力或传统 MLP 块。 论文报告 3B Mamba 超过同规模 Transformer、匹配约两倍规模模型，并称推理吞吐最高约 5 倍、长度线性扩展。 因此，本章采用它来支持这样的判断：贡献是让内容选择与线性状态更新结合成强通用骨干。

但它没有替我们回答所有问题。指标来自特定模型、硬件与 2023 基线；精确回忆和现代混合架构需重新测试。

## 一次有约束的选择：百万日志序列异常定位

任务既要检测长期趋势，也要精确找回某次关键错误。比较窗口 Transformer、Mamba 和混合层；数据按真实会话长度分层。指标包括异常 F1、关键事件 needle、多跳依赖、tokens/s、峰值显存和长序列衰减。若 SSM 吞吐领先但精确回忆失败，可用层次索引或局部 attention，而不是宣称架构绝对胜负。

### 读案例时做三次停顿

1. 到 注意力 时停一次：任务是否需要任意位置直接交互？
2. 到 高效实现 时再停一次：哪条数据流、状态变化或约束能证明机制真的发生？
3. 到 选择性 SSM 时最后停一次：如果出现“把 $O(n)$ 当所有硬件都更快”，系统应继续、拒绝还是交给人？

## 做一次公平对照：让线性架构和注意力各赢一次

### 第 1 步：手算四 token 单头注意力和 mask

- **这一步在验证：** 注意力能否支持“任务是否需要任意位置直接交互”这项判断。
- **至少留下：** 主指标、切片、置信区间与失败样本。
- **先停下来而不是继续调参的信号：** 把 $O(n)$ 当所有硬件都更快。

### 第 2 步：在同一参数/训练 token 下比较小 Transformer 与 Mamba

- **这一步在验证：** 高效实现能否支持“瓶颈在 FLOPs、带宽还是容量”这项判断。
- **至少留下：** loss、梯度/激活、吞吐、显存、数据量、FLOPs。
- **先停下来而不是继续调参的信号：** 用 needle 检索代表长程推理。

### 第 3 步：按 1k/8k/32k 长度报告质量、吞吐和内存

- **这一步在验证：** 选择性 SSM能否支持“长序列和内容检索如何权衡”这项判断。
- **至少留下：** 首 Token、每 Token、P50/P95、吞吐、峰值内存。
- **先停下来而不是继续调参的信号：** 忽略 KV cache 精度和并发。

### 第 4 步：加入精确复制与长期聚合两类合成任务定位偏置

- **这一步在验证：** 注意力能否支持“任务是否需要任意位置直接交互”这项判断。
- **至少留下：** 校准、偏移、攻击、记忆/污染与能耗。
- **先停下来而不是继续调参的信号：** 把 attention map 当解释。

完成示范后再做一次变式：构造一个长期平均任务和一个任意位置精确复制任务，在多长度下对照两类模型；解释归纳偏置而非只报总平均。

<details class="ai-workbench">
<summary>让 AI 生成候选，不让它跳过取舍</summary>

~~~~text
请为序列任务比较 Transformer、FlashAttention 实现、选择性 SSM 与混合架构。固定参数、训练 token、数据和硬件；按长度报告任务质量、精确回忆、聚合、多跳、吞吐、首 token、峰值显存和 KV/状态容量；指出 IO、FLOPs 与内容记忆的不同瓶颈。
~~~~

先让 AI 复述任务、证据和禁止外推的范围，再接受它生成的方案。
</details>

## 什么信号会推翻当前选择

先排除这些误解：注意力天然可解释；FlashAttention 是近似；线性复杂度必然低延迟；百万上下文等于百万有效推理；KV cache 只与参数量有关。

### 把 $O(n)$ 当所有硬件都更快

- **先看什么：** 主指标、切片、置信区间与失败样本
- **回到哪项控制：** 序列骨干选择按任务记忆类型与长度分布
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“让线性架构和注意力各赢一次”的回归记录。

### 用 needle 检索代表长程推理

- **先看什么：** loss、梯度/激活、吞吐、显存、数据量、FLOPs
- **回到哪项控制：** 训练和推理分别测 IO 与内存
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“让线性架构和注意力各赢一次”的回归记录。

### 忽略 KV cache 精度和并发

- **先看什么：** 首 Token、每 Token、P50/P95、吞吐、峰值内存
- **回到哪项控制：** 长上下文宣传需通过检索、聚合和缺失信息测试
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“让线性架构和注意力各赢一次”的回归记录。

### 把 attention map 当解释

- **先看什么：** 校准、偏移、攻击、记忆/污染与能耗
- **回到哪项控制：** 混合架构保留可消融层比例
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“让线性架构和注意力各赢一次”的回归记录。

### 比较不同训练数据/参数的架构

- **先看什么：** 主指标、切片、置信区间与失败样本
- **回到哪项控制：** 序列骨干选择按任务记忆类型与长度分布
- **怎样留下证据：** 保存触发条件、原始输入、系统响应和人工处置；修复后把同一样本加入“让线性架构和注意力各赢一次”的回归记录。

## 证据改变时怎样重选

### 1. 序列骨干选择按任务记忆类型与长度分布

验收时查看 **任务质量**，至少保存主指标、切片、置信区间与失败样本。如果观察到“把 $O(n)$ 当所有硬件都更快”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 2. 训练和推理分别测 IO 与内存

验收时查看 **训练**，至少保存loss、梯度/激活、吞吐、显存、数据量、FLOPs。如果观察到“用 needle 检索代表长程推理”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 3. 长上下文宣传需通过检索、聚合和缺失信息测试

验收时查看 **推理**，至少保存首 Token、每 Token、P50/P95、吞吐、峰值内存。如果观察到“忽略 KV cache 精度和并发”，这项决定必须重新评审，不能因为已经实现就默认保留。

### 4. 混合架构保留可消融层比例

验收时查看 **风险**，至少保存校准、偏移、攻击、记忆/污染与能耗。如果观察到“把 attention map 当解释”，这项决定必须重新评审，不能因为已经实现就默认保留。

## 🎯 随堂检验

<Quiz question="FlashAttention 为什么能在数学结果近似不变时提速？" :options='["删除大部分 token","分块减少 HBM 与片上内存之间的 IO","把注意力换成卷积"]' :answer="1" explanation="核心是 IO-aware 精确计算。" />

## 本章小结

- 注意力提供全局内容寻址
- FlashAttention 优化 IO 而非改变目标
- Mamba 用输入选择压缩历史
- 真实选型需同时测记忆类型和系统性能

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释attention 与选择性 SSM 的状态差异，并完成一个按长度分层的质量/吞吐对照。</p>
  <p><strong>真正会用：</strong>换一个场景仍能指出线性复杂度不保证更优的条件，同时保留失败样本或拒绝条件。</p>
</div>

<EvidenceTracker lesson="field-deep-learning-04-sequence-transformer-ssm" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/2205.14135">FlashAttention</a>、<a href="https://arxiv.org/abs/2312.00752">Mamba</a>。涉及新模型、法规或标准时，请按链接中的版本和适用范围复核。</div>
