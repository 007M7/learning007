# 02 · 把神经网络画成计算图

> 前向值、局部导数与依赖怎样组成可求导程序？

<div class="lesson-meta"><span>DLF02</span><span>阶段一 · 张量、梯度与稳定训练</span><span>概念辨析课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>计算图与局部 Jacobian 标注。</p>
  <p><strong>最低前置：</strong>需要能计算向量、矩阵乘法、导数和复合函数；不会手推全部矩阵微积分也可以，但要理解维度和局部梯度。</p>
</div>

## 为什么现在要学“把神经网络画成计算图”

很多误判并非算错，而是把两个相近词当成同一件事。围绕 **计算图**，本章先建立可反驳的定义。

**本章核心判断：** 计算图把值与依赖显式化，局部算子只需保存反向所需状态就能参与整体求导。

本章只追一个判断：**前向值、局部导数与依赖怎样组成可求导程序？** 最后必须用“计算图与局部 Jacobian 标注”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 给“把神经网络画成计算图”一个可操作定义

计算图把值与依赖显式化，局部算子只需保存反向所需状态就能参与整体求导。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **计算图与局部 Jacobian 标注**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 张量与算子 | 带形状、dtype、设备和布局的多维数据及变换 | 相信能运行就维度正确 |
| 当前支点 | 计算图 | 哪些值需保留、重算或停止梯度 | 梯度存在说明目标正确 |
| 下游 | 自动微分 | 以 JVP/VJP 组合精确传播导数 | 把数值差分误差当 AD 错误 |

现在不用背表。请把三行连成一句因果解释，再用 **把神经网络画成计算图** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 用公式审查“把神经网络画成计算图”

若 $z=f(x,y)$、$L=g(z)$，链式法则给出

$$
\frac{\partial L}{\partial x}=\frac{\partial L}{\partial z}\frac{\partial z}{\partial x}.
$$

多层网络 $h_l=\phi(W_lh_{l-1}+b_l)$ 的伴随递推为

$$
\bar h_{l-1}=W_l^T\left(\bar h_l\odot\phi'(a_l)\right),
\quad \frac{\partial L}{\partial W_l}=(\bar h_l\odot\phi'(a_l))h_{l-1}^T.
$$

逐个把符号放回真实任务，并检查：

- 广播维度在反向时需沿扩展轴求和
- 非标量输出反向本质是 VJP，需要上游向量
- in-place 修改可能破坏保存的中间量
- 数值梯度只适合小规模、双精度和避开不可微点

推导完成不代表结论成立。至少改变一次关键条件，观察 **计算图** 的结论是否保持；若不保持，把条件写入“计算图与局部 Jacobian 标注”而不是藏在代码默认值里。

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“前向值、局部导数与依赖怎样组成可求导程序？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“张量的形状、步幅与广播”提供前置条件，本章用 **计算图** 解释关键机制，并把结果留在“计算图与局部 Jacobian 标注”中。下一主题“反向传播就是复用链式法则”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“在需要梯度的路径 detach”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“训练循环显式区分 forward、backward、step、zero_grad”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **计算图与局部 Jacobian 标注**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 张量与算子 怎样经过 计算图 影响结果

前向执行把输入沿算子图变成损失，并保存反向所需的局部信息；反向从标量损失的伴随 1 出发，对每个算子应用向量—Jacobian 乘积，将来自多条路径的梯度相加。神经网络参数很多、损失很少，因此反向模式比逐参数前向模式高效。checkpointing 用重算换激活内存，`no_grad` 与 detach 改变图的边界。

~~~~mermaid
flowchart LR
  X[输入张量] --> O1[算子/形状/dtype]
  O1 --> H[中间激活]
  H --> L[标量损失]
  L --> V[VJP 反向]
  V --> G[参数梯度累积]
  G --> C[梯度检查/裁剪]
  C --> U[优化器更新]
~~~~

读图时从结果逆向追问：哪一步能够观察，哪一步只是假设，哪一步失效后必须让系统拒绝或降级。

## 两份证据把“把神经网络画成计算图”的边界画在哪里

### Automatic Differentiation in Machine Learning: a Survey（2018-10-17）

本章用它校准“把神经网络画成计算图”的边界：贡献是让反传被理解为通用程序变换而非神经网络特例。。**不要越过的边界：**综述不替代具体框架在随机、控制流、复数和分布式算子上的实现文档。

### PyTorch: An Imperative Style, High-Performance Deep Learning Library（2019-12-05）

本章用它校准“把神经网络画成计算图”的边界：贡献是建立现代研究代码的执行模型。。**不要越过的边界：**框架持续演进，论文中的编译与分布式能力已非 2026 完整状态；必须查当前官方文档。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| PyTorch: An Imperative Style, High-Performance Deep Learning Library | 带着“前向值、局部导数与依赖怎样组成可求导程序？”阅读：PyTorch 以 define-by-run 构建动态图，autograd 记录运行时操作；后端张量库、设备抽象和分布式组件承载性能。 | 框架持续演进，论文中的编译与分布式能力已非 2026 完整状态；必须查当前官方文档。 |

建议先读教材或标准中与 **把神经网络画成计算图** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：Deep Learning、Autodiff、PyTorch。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 把“把神经网络画成计算图”做成可重放实验

实验标题：**计算图与局部 Jacobian 标注**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“前向值、局部导数与依赖怎样组成可求导程序？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 把数值差分误差当 AD 错误。

### 2. 执行本章关键动作

与框架 autograd 对比每个参数梯度

- **必须保存：** loss、梯度/激活、吞吐、显存、数据量、FLOPs。
- **停止信号：** 跨 batch 意外累积梯度。

### 3. 主动注入反例

构造“把数值差分误差当 AD 错误”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 在需要梯度的路径 detach。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 计算图与局部 Jacobian 标注及其适用范围。
- **停止信号：** 只监测 loss 不监测梯度和激活。

## 当 计算图 失效，先查哪里

先排除这些看似合理的误解：自动微分等于符号推导；梯度存在说明目标正确；反向传播只能用于神经网络；detach 只节省内存；loss 下降能排除实现错误。

### 把数值差分误差当 AD 错误

先查看 **训练**：loss、梯度/激活、吞吐、显存、数据量、FLOPs。如果证据确认失败，回到“训练循环显式区分 forward、backward、step、zero_grad”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 在需要梯度的路径 detach

先查看 **风险**：校准、偏移、攻击、记忆/污染与能耗。如果证据确认失败，回到“混合精度与 checkpointing 单独做数值/性能回归”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “计算图与局部 Jacobian 标注”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 训练循环显式区分 forward、backward、step、zero_grad | loss、梯度/激活、吞吐、显存、数据量、FLOPs | 把数值差分误差当 AD 错误 |
| 关键自定义算子附 gradcheck | 首 Token、每 Token、P50/P95、吞吐、峰值内存 | 跨 batch 意外累积梯度 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“计算图与局部 Jacobian 标注”，否则下一次改动只能重新争论。

## 把“把神经网络画成计算图”迁移到新条件

围绕“把神经网络画成计算图”完成如下变式：实现线性层＋ReLU＋MSE 的前后向，随机抽五个参数做数值梯度；再制造一个广播 bug，解释为何 loss 仍可能下降。

提交“计算图与局部 Jacobian 标注”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“把神经网络画成计算图”：前向值、局部导数与依赖怎样组成可求导程序？ 预期产物是计算图与局部 Jacobian 标注。
请把这段训练代码画成张量计算图，逐算子标注 shape/dtype/device、需要保存的激活和梯度路径；检查广播、detach、in-place、累积与混合精度；为最小样本手算前后向并设计 central-difference gradcheck。不要先建议换模型。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="学习“把神经网络画成计算图”时，哪种做法最不可信？" :options='["梯度存在说明目标正确","完成计算图与局部 Jacobian 标注并保留反例","先冻结任务和评价口径"]' :answer="0" explanation="“梯度存在说明目标正确”忽略了本章的机制或边界。" />

## 本章小结

- **问题：** 前向值、局部导数与依赖怎样组成可求导程序？
- **机制：** 深度学习训练的第一性对象不是层的名字，而是张量值、计算依赖和梯度怎样穿过每个算子。
- **产物：** 计算图与局部 Jacobian 标注
- **边界：** AD 正确但学习目标仍错误的情形

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释反向模式如何沿图传播 VJP，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成计算图与局部 Jacobian 标注，保留失败样本，并说明AD 正确但学习目标仍错误的情形。</p>
</div>

<EvidenceTracker lesson="field-deep-learning-02-computation-graph" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/1502.05767">AD Survey</a>、<a href="https://arxiv.org/abs/1912.01703">PyTorch</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
