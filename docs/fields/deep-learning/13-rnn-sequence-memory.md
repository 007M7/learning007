# 13 · RNN 的状态、记忆与梯度路径

> 序列压进固定状态时，哪些信息最先丢失？

<div class="lesson-meta"><span>DLF13</span><span>阶段二 · 结构归纳偏置</span><span>原理推导课</span><span>证据更新至 2026-08-30</span></div>

<div class="learning-brief">
  <p><strong>本章不是要记住：</strong>一串术语或某个工具的默认参数。</p>
  <p><strong>本章完成时要交付：</strong>展开计算图与记忆压力测试。</p>
  <p><strong>最低前置：</strong>需掌握矩阵乘法、残差/归一化、序列张量和基本复杂度。</p>
</div>

## 为什么现在要学“RNN 的状态、记忆与梯度路径”

公式不是装饰。它的价值是迫使我们写清对象、假设和优化目标，再检查现实是否满足。

**本章核心判断：** RNN 把历史压缩进递归状态，长路径上的梯度与有限状态容量共同限制远程记忆。

本章只追一个判断：**序列压进固定状态时，哪些信息最先丢失？** 最后必须用“展开计算图与记忆压力测试”来回答；如果产物不能暴露失败样本，就还不能算学会。

## 用公式审查“RNN 的状态、记忆与梯度路径”

缩放点积注意力：

$$
\operatorname{Attn}(Q,K,V)=\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}+M\right)V.
$$

离散状态空间模型：

$$
h_t=\bar A(x_t)h_{t-1}+\bar B(x_t)x_t,\qquad y_t=C(x_t)h_t.
$$

逐个把符号放回真实任务，并检查：

- softmax 权重是模型路由而非人类可解释因果
- FlashAttention 改变 IO 复杂度而非数学结果
- KV cache 大小随层、头、长度和 dtype 增长
- SSM 线性时间不保证精确任意位置检索

推导完成不代表结论成立。至少改变一次关键条件，观察 **注意力** 的结论是否保持；若不保持，把条件写入“展开计算图与记忆压力测试”而不是藏在代码默认值里。

## 给“RNN 的状态、记忆与梯度路径”一个可操作定义

RNN 把历史压缩进递归状态，长路径上的梯度与有限状态容量共同限制远程记忆。

可操作定义必须允许你回答三件事：输入或条件是什么，发生了哪条机制，最后观察什么才算支持结论。本章的观察出口是 **展开计算图与记忆压力测试**。

### 三个支撑概念，而不是本章的替代标题

| 位置 | 概念 | 在本章中的作用 | 一旦误用会怎样 |
|---|---|---|---|
| 前置 | 选择性 SSM | 输入依赖状态更新以线性扫描保留/遗忘信息 | 把 $O(n)$ 当所有硬件都更快 |
| 当前支点 | 注意力 | 任务是否需要任意位置直接交互 | 注意力天然可解释 |
| 下游 | 高效实现 | 通过分块、融合和缓存减少内存 IO/重复计算 | 用 needle 检索代表长程推理 |

现在不用背表。请把三行连成一句因果解释，再用 **RNN 的状态、记忆与梯度路径** 的场景举一个反例：哪些观测出现时，原解释必须被推翻？

## 沿着一条完整推理链走到底

### 第一步：固定要解释或改变的对象

不要用“整体效果更好”代替对象。把“序列压进固定状态时，哪些信息最先丢失？”改写为：在明确输入、时间点和约束下，观察什么量，比较什么基线，最后据此采取什么动作。

### 第二步：写出中间机制

上一主题“别只看 Top-1：视觉错误切片”提供前置条件，本章用 **注意力** 解释关键机制，并把结果留在“展开计算图与记忆压力测试”中。下一主题“自注意力：内容寻址的信息混合”只在这些证据成立后继续。 只要中间任一环只能凭主观解释，结论就应降级为假设。

### 第三步：主动寻找反例

先尝试制造“忽略 KV cache 精度和并发”。如果当前方法仍给出看似正常的平均分，说明评价没有覆盖真正风险，需要补切片、过程指标或拒绝条件。

### 第四步：把结论写成可撤回决定

当前决定是“序列骨干选择按任务记忆类型与长度分布”。同时写下适用条件、责任人和重新评审信号；这样新证据出现时能更新，而不是把旧结论当永久规则。

这条推理链最终必须落到 **展开计算图与记忆压力测试**。只有定义、机制、观察和决定互相对得上，文章内容才真正进入工作系统。

## 把“RNN 的状态、记忆与梯度路径”放回 40 章依赖链

~~~~mermaid
flowchart LR
  A[上一主题：别只看 Top-1：视觉错误切片] -->|提供前置| B[当前：RNN 的状态、记忆与梯度路径]
  B -->|留下：展开计算图与记忆压力测试| C[下一主题：自注意力：内容寻址的信息混合]
  C --> D{能否支持：展开计算图与记忆压力测试}
  D -->|不能| E[回到假设与数据]
  D -->|能够| F[进入下一主题]
~~~~

这张图不是通用真理，而是本章的待验证模型。序列架构的竞争不是 $O(n^2)$ 与 $O(n)$ 的口号，而是内容检索、状态压缩、硬件利用和真实长度分布的联合问题。 如果输入、环境或责任边界改变，箭头也要重新检查。

## 把“RNN 的状态、记忆与梯度路径”做成可重放实验

实验标题：**展开计算图与记忆压力测试**。不要同时追求完整系统，只要求输入可重放、判断可观测、失败可解释。

### 1. 冻结问题和验收

把“序列压进固定状态时，哪些信息最先丢失？”写成输入、条件、基线和通过阈值，禁止实验后改题。

- **必须保存：** 问题卡、数据/环境版本和预期失败。
- **停止信号：** 把 $O(n)$ 当所有硬件都更快。

### 2. 执行本章关键动作

手算四 token 单头注意力和 mask

- **必须保存：** 主指标、切片、置信区间与失败样本。
- **停止信号：** 用 needle 检索代表长程推理。

### 3. 主动注入反例

构造“把 $O(n)$ 当所有硬件都更快”，验证评价或安全控制是否真的能发现。

- **必须保存：** 触发输入、完整过程和系统响应。
- **停止信号：** 忽略 KV cache 精度和并发。

### 4. 比较并做决定

把结果与两份来源的任务和边界对齐，决定继续、降级、拒绝或补证据。

- **必须保存：** 展开计算图与记忆压力测试及其适用范围。
- **停止信号：** 把 attention map 当解释。

## 两份证据把“RNN 的状态、记忆与梯度路径”的边界画在哪里

### FlashAttention（2022-06-23）

这项工作追问：能否通过 IO-aware 精确算法加速注意力并降低内存？

算法把 $Q,K,V$ 分块，在 SRAM 中在线维护 softmax 统计，避免物化完整注意力矩阵；并给出 HBM IO 分析。 论文报告相对基线显著加速和节省内存，并支持更长上下文。

本章据此采用的判断是：**贡献是把硬件内存层级变成算法设计变量。** 但不能外推到：收益依 GPU、序列形状、内核和后续版本；不能把理论 FLOPs 当实际速度。

### Mamba: Linear-Time Sequence Modeling with Selective State Spaces（2023-12-01）

本章用它校准“RNN 的状态、记忆与梯度路径”的边界：贡献是让内容选择与线性状态更新结合成强通用骨干。。**不要越过的边界：**指标来自特定模型、硬件与 2023 基线；精确回忆和现代混合架构需重新测试。

两份来源若结论看似冲突，先比较任务定义、数据分布、资源预算和评价方法；不能只用发布日期或排行榜高低裁决。

## 怎样读本章来源，而不是收藏链接

| 阅读对象 | 带着什么问题读 | 读完不能声称什么 |
|---|---|---|
| FlashAttention | 带着“序列压进固定状态时，哪些信息最先丢失？”阅读：算法把 $Q,K,V$ 分块，在 SRAM 中在线维护 softmax 统计，避免物化完整注意力矩阵；并给出 HBM IO 分析。 | 收益依 GPU、序列形状、内核和后续版本；不能把理论 FLOPs 当实际速度。 |

建议先读教材或标准中与 **RNN 的状态、记忆与梯度路径** 对应的定义和假设，再读论文的方法、比较基线与限制，最后回到自己的产物重做一次。阅读笔记只保留三类信息：改变了哪个判断、补了哪项实验、增加了哪条边界。不能改变这三项的摘抄暂不进入知识库。

本章的来源名称还包括：Attention Is All You Need、FlashAttention、Mamba。它们构成继续深挖的入口，不代表每一项都已被同等强度地验证。

## 当 注意力 失效，先查哪里

先排除这些看似合理的误解：注意力天然可解释；FlashAttention 是近似；线性复杂度必然低延迟；百万上下文等于百万有效推理；KV cache 只与参数量有关。

### 把 $O(n)$ 当所有硬件都更快

先查看 **任务质量**：主指标、切片、置信区间与失败样本。如果证据确认失败，回到“序列骨干选择按任务记忆类型与长度分布”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

### 忽略 KV cache 精度和并发

先查看 **推理**：首 Token、每 Token、P50/P95、吞吐、峰值内存。如果证据确认失败，回到“长上下文宣传需通过检索、聚合和缺失信息测试”，保存触发输入、系统响应和人工处置，再把样本加入回归集。

本章的边界不是一句“视情况而定”，而是能写进测试、监测或人工审核条件的触发器。

## “展开计算图与记忆压力测试”怎样进入真实系统

| 决定 | 验收依据 | 重新评审的信号 |
|---|---|---|
| 序列骨干选择按任务记忆类型与长度分布 | 主指标、切片、置信区间与失败样本 | 把 $O(n)$ 当所有硬件都更快 |
| 训练和推理分别测 IO 与内存 | loss、梯度/激活、吞吐、显存、数据量、FLOPs | 用 needle 检索代表长程推理 |

这些不是永久最佳实践，而是当前证据下的可逆决定。把决定、理由、适用范围和退出信号一起写进“展开计算图与记忆压力测试”，否则下一次改动只能重新争论。

## 把“RNN 的状态、记忆与梯度路径”迁移到新条件

围绕“RNN 的状态、记忆与梯度路径”完成如下变式：构造一个长期平均任务和一个任意位置精确复制任务，在多长度下对照两类模型；解释归纳偏置而非只报总平均。

提交“展开计算图与记忆压力测试”时同时附上：

1. 一个成功样本和一个失败样本；
2. 失败推翻了哪条假设，而不只是报错信息；
3. 哪项结论来自公开来源，哪项只来自你的本地实验；
4. 如果换数据、人群、环境或本体，最先需要重测什么；
5. 当前仍无法消除的残余风险。

<details class="ai-workbench">
<summary>让 AI 做反方审稿人</summary>

~~~~text
本次只审查“RNN 的状态、记忆与梯度路径”：序列压进固定状态时，哪些信息最先丢失？ 预期产物是展开计算图与记忆压力测试。
请为序列任务比较 Transformer、FlashAttention 实现、选择性 SSM 与混合架构。固定参数、训练 token、数据和硬件；按长度报告任务质量、精确回忆、聚合、多跳、吞吐、首 token、峰值显存和 KV/状态容量；指出 IO、FLOPs 与内容记忆的不同瓶颈。

请额外指出：我的结论依赖哪些隐藏前提？给出一个最小反例和一个能推翻结论的实验。不要替我伪造实验结果或来源。
~~~~
</details>

## 🎯 随堂检验

<Quiz question="FlashAttention 为什么能在数学结果近似不变时提速？" :options='["删除大部分 token","分块减少 HBM 与片上内存之间的 IO","把注意力换成卷积"]' :answer="1" explanation="核心是 IO-aware 精确计算。" />

## 本章小结

- **问题：** 序列压进固定状态时，哪些信息最先丢失？
- **机制：** 序列架构的竞争不是 $O(n^2)$ 与 $O(n)$ 的口号，而是内容检索、状态压缩、硬件利用和真实长度分布的联合问题。
- **产物：** 展开计算图与记忆压力测试
- **边界：** 线性复杂度不保证更优的条件

<div class="learning-brief">
  <p><strong>基础掌握：</strong>不看正文解释attention 与选择性 SSM 的状态差异，并能指出它与本章主题的关系。</p>
  <p><strong>真正会用：</strong>独立完成展开计算图与记忆压力测试，保留失败样本，并说明线性复杂度不保证更优的条件。</p>
</div>

<EvidenceTracker lesson="field-deep-learning-13-rnn-sequence-memory" />

<div class="source-note">资料核验至 2026-08-30：<a href="https://arxiv.org/abs/2205.14135">FlashAttention</a>、<a href="https://arxiv.org/abs/2312.00752">Mamba</a>。本章只采用来源能够支持的机制和边界；模型、法规、标准或产业状态变化后必须重新核验。</div>
